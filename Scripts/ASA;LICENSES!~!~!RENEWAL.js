showMessage = true;
showDebug = true;

//Yazan Barghouth: this script was moved to STDBASE_COPY_RECORD_DATA, executed when Renewal=true in JSON
//if (parentCapId != "" && parentCapId != null) {
//	var result = aa.cap.createRenewalCap(parentCapId, capId, true);
//}